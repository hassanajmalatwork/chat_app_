import 'package:flutter/material.dart';

class chatMessges extends StatelessWidget {
  const chatMessges({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No messeges found'),
    );
  }
}
